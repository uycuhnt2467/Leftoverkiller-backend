FROM mysql:5.7

ARG ROOT_PASS_WORD
ARG DATABASE
ARG DATABASE_USER
ARG DATABASE_PASSWORD

ENV MYSQL_ROOT_PASSWORD $ROOT_PASS_WORD
ENV MYSQL_DATABASE $DATABASE
ENV MYSQL_USER $DATABASE_USER
ENV MYSQL_PASSWORD $DATABASE_PASSWORD

COPY ./init.sql /docker-entrypoint-initdb.d
EXPOSE 3306